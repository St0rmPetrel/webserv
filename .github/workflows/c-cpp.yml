name: C/C++ CI

on:
  push:
    branches: [ CI ]
  pull_request:
    branches: [ * ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: logger_test
      run: ./srcs/logger/test.sh
